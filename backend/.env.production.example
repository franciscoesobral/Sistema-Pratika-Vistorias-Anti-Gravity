# Vercel Postgres (adicionar via Vercel Dashboard -> Storage -> Create Database -> Postgres)
# A variável POSTGRES_URL será automaticamente injetada pelo Vercel
DATABASE_URL="${POSTGRES_URL}"

# Google Drive Integration (opcional para início)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
DRIVE_FOLDER_ID=

# Email Service (opcional para início)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_DESTINO_RELATORIOS=

# Server
PORT=3333
FRONTEND_URL="https://seu-frontend.vercel.app"

# Security (GERAR NOVO EM PRODUÇÃO!)
JWT_SECRET="CHANGE_ME_IN_PRODUCTION_USE_STRONG_RANDOM_STRING"
